# JDBC

## 실습 

### 실습 1) 스프링부트 프로젝트를 생성하시오. 

- 필수 라이브러리 추가 : web, postgresql driver, docker compose support, jdbc

### 실습 2) application.properties에 다음 내용을 추가하시오

```properties
spring.docker.compose.lifecycle-management=start-only
spring.sql.init.mode=always
```

### 실습 3) schema.sql 의 내용에 다음 내용을 추가하시오.

```sql
-- Create the student table
CREATE TABLE student (
    student_id INT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    age INT NOT NULL
);

-- Create the student_grades table
CREATE TABLE student_grades (
    student_id INT NOT NULL,
    subject VARCHAR(20) NOT NULL,
    grade INT NOT NULL,
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);
```

### 실습 4) data.sql 의 내용에 다음을 추가하시오.

```sql
-- Insert sample data into the student_grades table
INSERT INTO student_grades (student_id, subject, grade)
VALUES
    (1,'economics', 80),
    (1,'math', 90),
    (1,'education', 95),
    (2,'physics', 85),
    (2,'math', 90),
    (3,'statistics', 75),
    (3,'math', 80),
    (4,'math', 90),
    (4,'education', 95),
    (5,'math', 85);

-- Insert sample data into the student table
INSERT INTO student (student_id, name, age)
VALUES
    (1, 'John Doe', 22),
    (2, 'Jane Doe', 24),
    (3, 'Jim Smith', 21),
    (4, 'Sarah Johnson', 23),
    (5, 'Mike Brown', 20);
```

### 실습 5) 실습3, 4에 작성된 스키마를 참조하는 restAPI를 작성하시오. 

- crud는 기본으로 구현하시오.
- 학생별 평균을 제공하는 api를 만드시오