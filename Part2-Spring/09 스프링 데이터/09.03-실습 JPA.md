# JPA

## 실습

> 💡jpa ddl-auto에 대한 더 자세한 강의
> - github
>   https://github.com/ohhoonim/demo-jpa.git
> - JPA의 DDL-AUTO가 만들어내는 DDL은 어떤 모습일까
>   https://youtu.be/LAsT3LTINKo

[참고]
```properties
# application.properties

spring.application.name=demo-jpa

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

spring.sql.init.mode=never

spring.datasource.url=jdbc:postgresql://localhost:5432/mydatabase
spring.datasource.username=myuser
spring.datasource.password=secret
spring.datasource.driver-class-name=org.postgresql.Driver

logging.level.org.hibernate.orm.jdbc.bind=TRACE

spring.docker.compose.lifecycle-management=start-only
```

### 실습 1) 스프링부트 프로젝트 생성하기

- 필수 디펜던시 
  : web, docker-compose, postgre, data-jpa 

### 실습 2) 다음의 note 클래스를 참고하여 rest api를 작성하시오. 

- crud 에 대한 rest api를 작성하시오
- 실제 데이터를 이용해야합니다.
- 데이터 조작은 jpa를 이용하시오. 

```java
public class Note {
    private long id;
    private String title;
    private String content;
}
```