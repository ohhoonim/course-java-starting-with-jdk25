# 테스트 코드 작성하며 개발하기

## Spring Security demo소스

> 🗒️ github : demo-security-filter-chain
> https://github.com/ohhoonim/demo-security-filter-chain.git

## Spring Security 조금 더 자세한 강의 영상

> :movie_camera: Spring Security (코딩하는오후, 학습프로그램 )
> https://www.youtube.com/playlist?list=PLeMeDIV7bypvMs-SN1jvj2co3DCAdHWGP

## Security test 라이브러리 

```gradle
testImplementation 'org.springframework.security:spring-security-test'
```

## @WithMockUser

```java
@Test
@WithMockUser("ohhoonim")
public void entpointTest() {
    mockMvcTester.get().uri("/main")
            .assertThat()
            .hasStatus(200);
}
```

## @TestContainer

```java
@TestConfiguration
@EnableWebSecurity(debug = true)
public static class SecurityConfig {
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http.authorizeHttpRequests(authz -> authz
                .anyRequest().authenticated());
        return http.build();
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return PasswordEncoderFactories.createDelegatingPasswordEncoder(); 
    }

    @Bean UserDetailsService userDetailsService() {
        return new InMemoryUserDetailsManager(
                User.withUsername("ohhoonim")
                        .password(passwordEncoder().encode("password"))
                        .roles("USERB") // roles 또는 authorities 중 하나만 써야함. 나중에 적용한것이 덮어씀
                        .authorities("TEAM_A", "ROLE_USER")
                        .build());
    }
}
```

## [⁉️ 실습 하기 (click)](08.03-실습%20테스트%20코드%20작성하며%20개발하기.md)